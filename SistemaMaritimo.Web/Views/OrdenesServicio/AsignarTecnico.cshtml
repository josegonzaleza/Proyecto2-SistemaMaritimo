@model SistemaMaritimo.Web.Models.AsignarTecnicoViewModel

@{
    ViewData["Title"] = "Asignar Técnico";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-header bg-secondary text-white">
                <h4 class="mb-0">Asignar Técnico</h4>
            </div>
            <div class="card-body">
                @if (ViewBag.Error != null)
                {
                    <div class="alert alert-danger">@ViewBag.Error</div>
                }

                <form asp-action="AsignarTecnico" method="post">
                    <input asp-for="OrdenId" type="hidden" />

                    <div class="mb-3">
                        <label asp-for="PersonalId" class="form-label">Técnico</label>
                        <select asp-for="PersonalId" class="form-select" asp-items="ViewBag.Tecnicos">
                            <option value="">-- Seleccione un técnico --</option>
                        </select>
                        <span asp-validation-for="PersonalId" class="text-danger"></span>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-secondary">Asignar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>